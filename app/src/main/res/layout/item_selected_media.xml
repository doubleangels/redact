<!-- Check your item_selected_media.xml file -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="4dp">

    <ImageView
        android:id="@+id/mediaItemThumbnail"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:background="#EEEEEE"
        android:contentDescription="@string/content_description_image_view"
        android:scaleType="centerCrop"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ImageContrastCheck,VisualLintBounds" />

    <ImageView
        android:id="@+id/videoIndicator"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_video"
        android:visibility="gone"
        android:contentDescription="@string/content_description_image_view"
        app:layout_constraintBottom_toBottomOf="@id/mediaItemThumbnail"
        app:layout_constraintEnd_toEndOf="@id/mediaItemThumbnail"
        android:layout_margin="8dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
